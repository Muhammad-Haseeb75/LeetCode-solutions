class Solution {
public:
    int findClosest(int x, int y, int z) {
       int ans1 = abs(z - x);
       int ans2 = abs(z - y);

       if (ans1 == ans2) return 0;

       int ans = min(ans1 , ans2);

       if (ans == ans1) return 1;
       else return 2;
    }
};
